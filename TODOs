write docs

write tests

instrument local definitions if they have a sig

possible to cache the config file to prevent reading from disk every time?

fail to compile if config file is missing or invalid
